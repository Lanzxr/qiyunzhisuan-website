# Website维护日志

> 新条目放在最前；只新增，不改写历史结论。

## 2026-08-17 - 放大浏览器标签页 Logo 图标

### 目标

- 让标签页中的 Logo 在浏览器小尺寸图标区域保持清晰可辨。

### 根因或发现

- 原始 `qiyun-logo-concept-a.png` 是 `1024×1024` 画布，但可见图形约为 `760×507`，透明留白被浏览器一并缩放，导致实际 Logo 显得过小。

### 实施结果

- 从原始 Logo 的 alpha 边界裁切并生成 `assets/images/qiyun-favicon.png`，输出为 `64×64` PNG，保留 2px 内边距。
- `index.html` 改为引用该紧裁切资源，页面导航仍继续使用原始 Logo 文件。

### 文件

- `index.html`
- `assets/images/qiyun-favicon.png`
- `docs/technical/modules/website/README.md`
- `docs/technical/modules/website/MAINTENANCE_LOG.md`
- `docs/technical/MAINTENANCE_LOG.md`

### 验证

- 确认 favicon 输出尺寸为 `64×64`，可见图形绘制区域为 `60×40`，透明边界保留少量内边距。
- 确认 `<link rel="icon">` 唯一指向该资源，且现有发布流程会复制 `assets/`；`git diff --check` 通过。

### 已验证事实

- 页面 Logo 原始素材未改写；仅新增裁切后的 favicon 派生资源。

### 未验证边界

- 生产域名需在本次部署后重新截图确认；浏览器可能继续使用旧 favicon 缓存。

### 产品行为变化

- 有。标签页使用更紧凑的 Logo 图形，显示尺寸相较原始整幅 PNG 增大。

### 风险与回滚

- 风险较低；回滚时恢复 `index.html` 对原始 PNG 的引用并删除派生 favicon 文件即可。

## 2026-08-17 - 注册浏览器标签页 favicon

### 目标

- 让浏览器标签页显示公司 Logo，而不是默认的地球图标。

### 根因或发现

- Logo 只在页面导航正文中引用，`index.html` 的 `<head>` 原来没有 `rel="icon"`；线上域名的 `/favicon.ico` 返回 404。

### 实施结果

- 在 `index.html` 的 `<head>` 增加 PNG favicon 声明，复用 `assets/images/qiyun-logo-concept-a.png`。
- 现有静态发布流程整体复制 `assets/`，不需要新增发布白名单。

### 文件

- `index.html`
- `docs/technical/modules/website/README.md`
- `docs/technical/modules/website/MAINTENANCE_LOG.md`
- `docs/technical/MAINTENANCE_LOG.md`

### 验证

- 静态检查确认 `<link rel="icon">` 指向存在的 PNG 资源；`git diff --check` 通过。
- 线上旧版本的 Logo 图片可访问，修改后的线上页面与浏览器缓存需在部署后复核。

### 已验证事实

- Favicon 与页面 Logo 共用同一份本地概念素材；发布流程会携带该资源。

### 未验证边界

- 本次未执行生产部署后的真实浏览器标签页截图复核；浏览器可能缓存旧 favicon。

### 产品行为变化

- 有。支持 favicon 的浏览器在加载页面后会请求并显示公司 Logo。

### 风险与回滚

- 风险较低，主要受浏览器 favicon 缓存和深色标签栏对深色透明图标的对比度影响；回滚时移除新增的 `<link rel="icon">` 即可。

## 2026-08-17 - 明确语言与地区选择器展示

### 目标

- 按中国大陆场景优化页首语言入口，避免仅展示 `CN`、`TW`、`HK`、`EN` 等并列简称造成语言与地区含义不清，并解决原生下拉展开态缺少视觉层级的问题。

### 根因或发现

- 页面结构已经切换为原生 `select`，但脚本仍绑定旧 `.language-button`，导致切换交互未接通。
- 原生下拉菜单的展开面板由浏览器/操作系统绘制，默认白底蓝色高亮与近白导航的视觉系统不一致；仅依赖 CSS 无法稳定控制展开态，因此需要可访问的自定义列表框，同时保留原生后备。

### 实施结果

- 将四个语言按钮改为带“语言与地区”标签的自定义列表框，选项完整写出简体中文、繁体中文（中国台湾）、繁体中文（中国香港）和 English；保留原生 `select` 作为无脚本后备与状态源。
- 四种 locale 均同步本地化选择器标签、ARIA 名称和选项文案；台湾与香港文案使用“中国台湾/中國台灣”和“中国香港/中國香港”明确地区。
- 以浅灰胶囊背景、深色文字、细边框和焦点环增强触发器，展开面板使用白底、阴影、悬停和选中高亮，使其与白色导航保持同一层级；移动菜单中改为标签加选择框两列布局。
- 选择语言后同步 `html.lang`、URL 参数、本地存储和选择框值，并自动收起移动菜单。

### 文件

- `index.html`
- `assets/site.js`
- `assets/site.css`
- `docs/technical/modules/website/README.md`
- `docs/technical/modules/website/MAINTENANCE_LOG.md`
- `docs/technical/MAINTENANCE_LOG.md`

### 验证

- `node --check assets/site.js`、`node --check assets/animations.js` 和 `git diff --check` 通过。
- 本地 Chromium 在 `1280x720` 与 `390x844` 视口确认选择器尺寸、颜色、四种 locale 选项、`html.lang`、ARIA 文案和 URL 参数同步；移动菜单内选择后自动关闭。
- 两个视口的页面 `scrollWidth` 均未超过浏览器可用宽度；控制台未发现 error 或 warning。

### 已验证事实

- 语言入口不再以四个地区/语言代码并列展示；四个选项均包含可读语言名称，台湾和香港选项包含地区说明。
- 浅灰胶囊触发器与导航层级一致，展开面板使用同一浅色中性配色并支持键盘上下、Home/End、Enter/Space 和 Esc；原生 select 保留为无脚本后备与状态源。
- 本次未改变公司资料、服务内容、企业邮箱目标、隐私政策主体或静态部署边界。

### 未验证边界

- 无脚本后备的原生下拉展开面板最终绘制仍受操作系统、浏览器主题和平台控件限制，尚未在真实 iOS Safari、Android Chrome、Firefox 及 Windows 高对比模式中复核。
- 语言地区用词是按中国大陆场景采取的保守展示方式，未替代公司或法务对正式上线文案的最终确认。

### 产品行为变化

- 有。语言入口的可见结构、文案、颜色和移动菜单切换行为发生变化；语言仍在同一页面原地切换。

### 风险与回滚

- 风险主要在原生下拉面板的跨平台外观差异，以及较长地区文案在极窄视口的换行；回滚时恢复旧语言按钮结构、脚本绑定和对应 CSS 规则即可，不涉及数据迁移。

## 2026-08-17 - 移除首屏代码背景装饰内框

### 目标

- 移除用户指出的首屏右侧灰色线框，让代码背景以无框渐隐方式融入网格。

### 根因或发现

- 灰色线框来自 `.hero-background::after` 伪元素，仅承担装饰作用；在浅色代码背景上形成了不必要的矩形视觉边界。

### 实施结果

- 将 `.hero-background::after` 设为不显示，桌面和移动端不再绘制该矩形边框。
- 保留本地代码背景图片、左右渐隐、网格和首屏内容结构。

### 文件

- `assets/site.css`
- `docs/technical/modules/website/README.md`
- `docs/technical/modules/website/MAINTENANCE_LOG.md`
- `docs/technical/MAINTENANCE_LOG.md`

### 验证

- 本地 Chromium 桌面视口确认伪元素计算样式为 `display: none`，灰色内框消失，代码背景继续加载。
- 在 `390x844` 移动视口确认伪元素保持隐藏，页面 `scrollWidth` 为 `375px`，无横向溢出。
- `git diff --check` 通过。

### 已验证事实

- 本次仅移除首屏装饰内框，不改变背景素材、标题、CTA、导航或语言内容。

### 未验证边界

- 尚未在真实 iOS Safari、Android Chrome 和 Firefox 设备上复核不同显示器亮度下的背景渐隐效果。

### 产品行为变化

- 有。首屏右侧不再显示矩形装饰边框；页面功能和交互不变。

### 风险与回滚

- 风险较低，主要是右侧视觉锚点减少；回滚时恢复 `.hero-background::after` 的尺寸与边框规则即可。

## 2026-08-17 - 参考 Vercel 视觉语言重设计官网

### 目标

- 在保留公司内容、四语言原地切换、锚点、企业邮箱、本地图片和 GitHub Pages 发布模型的前提下，参考 Vercel 的克制产品技术风格重设计整站。

### 根因或发现

- 原页面同时存在紫色渐变、发光按钮、玻璃卡片、3D 卡片、粒子和多套圆角/阴影规则，视觉语言分散，导航、联系 CTA 和联系卡片与背景的层级不稳定。
- 参考站点的可复用设计信号是 64px 导航、近白背景、1px 中性分隔线、大字号中等字重标题、黑色主 CTA、白色次 CTA、低阴影和克制动效；不复制其品牌素材或内容。
- 新增的关于区标签缺少 `about.label` 四语言字段，英文页面会回退显示中文。
- 用户补充提供了 `qiyun-logo-concept-a.png` 概念标志；原页首只有文字品牌，缺少图形识别入口。

### 实施结果

- 首页改为近白网格与本地代码图片的左右信息结构，使用黑色主 CTA、白色次 CTA和单一蓝色文字品牌点缀。
- 企业信息、服务和隐私政策改用分隔线网格；移除可见的蓝紫渐变标题、浅紫列表卡片、发光和悬浮卡片效果。
- 关于区改为近黑背景、白色标题和线性原则列表，保留并重新平衡本地透明笔记本图片；联系区改为白底黑色 CTA 与近黑联系信息面板。
- 导航保留当前锚点状态与完整移动菜单交互，语言入口继续使用 `CN`、`TW`、`HK`、`EN`；补齐四种 locale 的 `about.label`。
- 将用户提供的 RGBA 概念标志复制到 `assets/images/`，以透明图片与文字品牌并列放入页首；桌面使用 52px 画布、窄屏使用 46px 画布，并保留文字品牌作为可读名称。
- 保留全部公司资料、服务内容、隐私政策、企业 `mailto` 地址、本地图片引用和静态发布边界。

### 文件

- `index.html`
- `assets/site.css`
- `assets/site.js`
- `assets/animations.js`
- `assets/images/qiyun-logo-concept-a.png`
- `docs/technical/modules/website/README.md`
- `docs/technical/modules/website/MAINTENANCE_LOG.md`
- `docs/technical/MAINTENANCE_LOG.md`

### 验证

- 本地 Chromium 在 `1280x720` 桌面视口检查首屏、服务、关于、联系和隐私区域；本地图片自然宽度为 `1200px` 且加载完成，页面无横向溢出。
- 在 `390x844` 移动视口检查首屏、菜单、服务、关于与联系区域；`scrollWidth` 为 `375px`，英文联系 CTA 宽 `335px`，无文字或控件溢出。
- 确认 Logo 图片自然宽度 `1024px`、RGBA 透明通道和加载完成；桌面导航图片框为 `52x52px`，移动导航图片框为 `46x46px`，菜单展开后仍无横向溢出。
- 实际点击四个语言入口，确认 `html.lang`、主标题、导航可访问名称和 `about.label` 分别更新；导航锚点、`aria-current="location"`、Esc 关闭菜单和 `mailto:cherishxr97@qiyunzhisuan.cn` 保持有效。
- 浏览器页面控制台无错误或警告；`node --check assets/site.js`、`node --check assets/animations.js` 与 `git diff --check` 通过。

### 已验证事实

- 最终可见页面不再使用紫色渐变作为主要界面语言，主色关系为近白、近黑、中性灰和单一蓝色品牌点缀。
- 四种 locale 均保留完整公司资料、服务、联系与隐私内容；关于区标签不再发生中文回退。
- 图形标志来自仓库内本地 PNG，不依赖运行时外部图片服务；页首仍保留本地化文字品牌和可访问名称。
- 本次未改变 GitHub Pages 工作流、公开 URL、企业邮箱目标、公司法定资料或用户数据处理范围。

### 未验证边界

- 尚未在真实 iOS Safari、Android Chrome、Firefox 和超宽屏设备上复核系统字体、触控反馈和图片色彩。
- 未实际打开本地邮件客户端，也未执行 GitHub Pages 生产部署或线上缓存刷新。

### 产品行为变化

- 有。整站视觉层级、组件样式、响应式排版、可见动效、关于区标签本地化和页首品牌呈现发生变化；业务内容、链接目标与静态发布模型不变。

### 风险与回滚

- 风险集中在旧 CSS 与最终覆盖层的级联维护成本、不同系统字体下的大标题换行，以及概念 Logo 尚未完成品牌规范确认；后续新增组件应优先使用最终中性色变量和分隔线规则。
- 回滚时恢复本次 `index.html`、`assets/site.css`、`assets/site.js`、`assets/animations.js` 与 Logo 图片改动即可，不涉及数据迁移、账号配置或部署工作流。

## 2026-08-17 - 优化联系信息卡片配色与对比度

### 目标

- 优化用户指出的联系信息卡片颜色，解决浅色玻璃底与白色文字叠加后显得发灰、信息不易扫描的问题。

### 根因或发现

- `.contact-details` 继承浅色 `glass-card` 背景，但标题、标签、邮箱和地址均使用白色系文字，卡片与联系区渐变及按钮的层级关系不清晰。

### 实施结果

- 联系信息卡片改为深靛色实体面板 `#11183f`，保留轻微内高光和阴影；左侧增加浅蓝色品牌边线以建立视觉锚点。
- 标题和正文使用白色层级，字段标签使用低透明度浅蓝色，企业邮箱使用浅青色强调；移除该卡片的背景模糊依赖。
- 未改变联系方式、邮箱链接、布局结构或移动端断点规则。

### 文件

- `assets/site.css`
- `docs/technical/modules/website/README.md`
- `docs/technical/modules/website/MAINTENANCE_LOG.md`
- `docs/technical/MAINTENANCE_LOG.md`

### 验证

- 本地 Chromium 在 `1331x1318` 桌面视口确认卡片背景为 `rgb(17, 24, 63)`、标题和正文清晰、页面无横向溢出。
- 在 `390x844` 移动视口确认卡片尺寸为 `335x319px`，邮箱、地址完整可读，页面 `scrollWidth` 为 `375px`。
- 浏览器控制台仅保留现有 GSAP `.scroll-arrow` 目标缺失警告；未发现本次卡片样式引入的错误。
- `node --check assets/site.js`、`node --check assets/animations.js` 与 `git diff --check` 通过。

### 已验证事实

- 联系信息卡片不再使用浅色半透明背景，文字颜色与卡片底色具有稳定对比度；邮箱仍指向 `mailto:cherishxr97@qiyunzhisuan.cn`。
- 其他 `glass-card` 组件和联系区 CTA 样式不因本次覆盖而改变。

### 未验证边界

- 尚未在真实 iOS Safari、Android Chrome 和 Firefox 设备上复核系统字体与触控表现。
- 未对真实邮件客户端进行发送操作。

### 产品行为变化

- 有。联系信息卡片的背景、边界和文字强调色发生变化；联系方式和业务流程不变。

### 风险与回滚

- 风险集中在不同显示器亮度和系统字体下的深色卡片观感；回滚时移除 `assets/site.css` 中 `.contact-details.glass-card` 覆盖规则即可，不涉及数据迁移或部署配置。

## 2026-08-17 - 分离联系区 CTA 与紫色渐变背景

### 目标

- 根据反馈进一步拉开“发送企业邮件”按钮与联系区渐变背景的视觉差异，避免按钮继续呈现为背景中的紫色块。

### 根因或发现

- 联系按钮仍带有通用 `neon-button` 类名，存在继承紫色渐变和发光规则的风险；即使覆盖部分属性，视觉语义仍与背景相似。

### 实施结果

- 将联系按钮改为独立的 `contact-cta` 类，移除 `neon-button` 与 `pulse` 类，使用纯白底、深靛色文字和 1px 边界。
- 保留桌面左对齐、窄屏撑满可用宽度、悬停上移和按下回落反馈；邮件目标和文案不变。

### 文件

- `index.html`
- `assets/site.css`
- `docs/technical/modules/website/MAINTENANCE_LOG.md`
- `docs/technical/MAINTENANCE_LOG.md`

### 验证

- 使用独立本地预览端口强制刷新资源，在 `1331x1318` 桌面视口确认按钮计算背景为纯白、无紫色渐变和持续动画。
- 在 `390x844` 移动视口确认按钮尺寸为 `335x54px`，`mailto` 地址保持正确，页面 `scrollWidth` 为 `375px`，没有横向溢出。
- 页面控制台未出现本次 CTA 样式相关错误；浏览器工具自身的网络统计超时不属于页面运行错误。
- `node --check assets/site.js`、`node --check assets/animations.js` 与 `git diff --check` 通过。

### 已验证事实

- 当前联系 CTA 不再使用 `neon-button` 或 `pulse` 类，不会继承全局紫色渐变按钮视觉。
- 联系区的公开邮件地址、语言绑定、导航锚点和数据处理范围均未改变。

### 未验证边界

- 尚未在真实 iOS Safari、Android Chrome 和 Firefox 设备上复核触控与邮件客户端唤起行为。
- 未实际点击邮件链接，避免在验证过程中打开本地邮件客户端。

### 产品行为变化

- 有。联系 CTA 的颜色、类名归属和视觉层级发生变化；邮件业务流程不变。

### 风险与回滚

- 风险集中在极窄视口下按钮文字与可用宽度的适配；回滚时恢复 `index.html` 的按钮类名和 `assets/site.css` 的 `contact-cta` 覆盖规则即可。

## 2026-08-17 - 优化联系区发送企业邮件按钮

### 目标

- 优化用户指出的“发送企业邮件”按钮，使 CTA 与联系文案层级更清晰，并在移动端保持易点按。

### 根因或发现

- 联系区按钮沿用全局居中的布局和持续脉冲阴影，在紫色背景中与装饰效果混在一起，未形成明确的行动落点。
- 全局焦点样式引用未定义的 `--neon-cyan` 变量，键盘焦点颜色存在回退风险。

### 实施结果

- 联系区 CTA 改为白色高对比主按钮，桌面端与正文左边界对齐，取消持续脉冲，仅保留悬停上移、按下回落和阴影反馈。
- 窄屏将按钮扩展为联系内容的可用宽度，保持 `52px` 以上触控高度；邮件 `mailto` 地址和可见文案不变。
- 将全局焦点轮廓改用已定义的 `--accent-cyan`，联系区按钮额外使用白色焦点轮廓以适配渐变背景。

### 文件

- `assets/site.css`
- `docs/technical/modules/website/README.md`
- `docs/technical/modules/website/MAINTENANCE_LOG.md`
- `docs/technical/MAINTENANCE_LOG.md`

### 验证

- 本地 Chromium 在 `1331x1318` 桌面视口确认按钮与正文左对齐、无持续动画；在 `390x844` 移动视口确认按钮宽 `335px`、高 `54px` 且页面 `scrollWidth` 未超过视口。
- `mailto:cherishxr97@qiyunzhisuan.cn` 链接和“发送企业邮件”可访问名称保持不变；按钮文字与白色背景对比度约 `8.82:1`。
- `node --check assets/site.js`、`node --check assets/animations.js` 与 `git diff --check` 通过。

### 已验证事实

- 本次只改变联系区 CTA 的视觉层级、布局和交互反馈，不改变邮件目标、语言字典、导航锚点或数据处理范围。
- 持续脉冲动画在联系区 CTA 上已关闭，`prefers-reduced-motion` 规则继续覆盖全站过渡动画。

### 未验证边界

- 尚未在真实 iOS Safari、Android Chrome 和 Firefox 设备上复核触控尺寸与邮件客户端唤起行为。
- 未实际点击邮件链接，避免在验证过程中打开或提交本地邮件客户端。

### 产品行为变化

- 有。联系区 CTA 的对齐方式、颜色层级、动效和窄屏宽度发生变化；邮件目标与业务流程不变。

### 风险与回滚

- 风险集中在渐变背景与不同系统字体下的按钮宽度；回滚时恢复 `assets/site.css` 中联系区按钮覆盖规则和焦点色变量即可，不涉及数据迁移或部署配置。

## 2026-08-17 - 优化关于区域笔记本图片融合

### 目标

- 优化用户指出的关于我们区域图片，让笔记本主体更清晰，同时消除图片边缘与页面背景之间的矩形感。

### 根因或发现

- 透明 PNG 的有效主体在原媒体框中占比偏小，桌面端笔记本与正文层级不够突出。
- 代码雨画布使用不透明清屏会逐帧累积成浅色矩形；图片边缘在放大后也需要额外的水平渐隐来避免直线裁切感。

### 实施结果

- 将 `.about-media` 保持为透明媒体层，桌面端放大图片并通过 `saturate`、`contrast` 保留屏幕与桌面光影细节。
- 为图片增加左右渐隐遮罩；移动端采用独立尺寸和居中定位，保持主体完整且不产生横向溢出。
- 将代码雨清屏改为 `destination-out` 透明擦除，并降低蓝紫字符透明度；画布增加 `aria-hidden`，图片增加懒加载和异步解码属性。

### 文件

- `index.html`
- `assets/site.css`
- `assets/animations.js`
- `docs/technical/modules/website/README.md`
- `docs/technical/modules/website/MAINTENANCE_LOG.md`
- `docs/technical/MAINTENANCE_LOG.md`

### 验证

- 本地 Chromium 在 `1331x1318` 桌面视口确认笔记本主体放大、背景无白色矩形；在 `390x844` 移动视口确认图片边缘自然、主体完整且页面 `scrollWidth` 未超过视口。
- 控制台仅保留现有 GSAP `.scroll-arrow` 目标缺失警告；未发现本次图片改动引入的错误。
- `node --check assets/site.js`、`node --check assets/animations.js` 与 `git diff --check` 通过。

### 已验证事实

- 关于区域图片继续使用仓库内 `assets/images/workstation-development-cutout.png`，未引入外部图片请求或新的构建步骤。
- 图片在桌面和窄屏均以透明媒体层渲染，代码雨不会填充为不透明矩形。

### 未验证边界

- 尚未在真实 iOS Safari、Android Chrome 和 Firefox 设备上复核遮罩和懒加载表现。
- 浏览器现有 `.scroll-arrow` GSAP 警告未在本任务中扩展修复。

### 产品行为变化

- 有。关于区域图片的视觉比例、边缘融合和装饰性代码雨呈现发生变化；文案、语言切换、导航锚点和数据处理不变。

### 风险与回滚

- 风险集中在极窄视口下图片主体与正文之间的垂直空间；回滚时恢复 `index.html` 图片属性、`assets/site.css` 的 `.about-media` 与图片规则，以及 `assets/animations.js` 的代码雨绘制段即可，不涉及数据迁移或部署配置。

## 2026-08-17 - 优化主导航层级与移动交互

### 目标

- 在不改变四个页面锚点、四种语言值和单页信息架构的前提下，提升主导航的视觉层级、当前状态、语言切换和移动端操作完整性。

### 根因或发现

- 导航样式存在重复的链接、语言按钮和菜单规则，后定义规则覆盖前定义规则，导致控件高度、颜色和分组关系不稳定。
- 移动菜单通过直接替换按钮文本切换图标，可能破坏本地化文字和可访问名称；关闭逻辑也没有覆盖点击外部、Esc 和返回桌面断点。
- 页面现有 GSAP 动画会改变长段落的几何位置，因此基于可视区域的滚动侦测会产生错误当前项；URL 锚点是本页更稳定的导航状态来源。

### 实施结果

- 将桌面导航收敛为 68px 高的单行结构，导航链接使用克制的悬停背景和当前项下划线，语言入口改为独立分段选择器。
- 860px 及以下使用浅色浮层菜单和 CSS 菜单图标，增加点击外部关闭、Esc 关闭并恢复焦点、语言选择后关闭，以及返回桌面断点时复位。
- 当前导航项只跟随明确的 URL 锚点和导航点击，并以 `aria-current="location"` 暴露；语言按钮继续使用 `CN/TW/HK/EN`，完整可访问名称随 locale 更新。

### 文件

- `index.html`
- `assets/site.css`
- `assets/site.js`
- `docs/technical/modules/website/README.md`
- `docs/technical/modules/website/MAINTENANCE_LOG.md`
- `docs/technical/MAINTENANCE_LOG.md`

### 验证

- `node --check assets/site.js`、`node --check assets/animations.js` 和 `git diff --check` 通过；导航结构断言与可见文案破折号检查通过。
- 本地 Chromium 在 `1331x1318` 桌面视口验证导航高度为 69px、当前 `#about` 正确、无横向溢出；在 `390x844` 验证菜单展开、语言切换、点击关闭、Esc 关闭和焦点恢复。
- 英文 locale 在 `1024x768` 保持四个导航链接单行，品牌、链接和四个语言入口均未溢出；浏览器控制台没有导航相关错误。

### 已验证事实

- 桌面导航和语言选择器在 860px 以上保持同一行，860px 及以下由菜单按钮承载。
- 四种语言切换继续原地更新页面和 URL 查询参数；语言按钮的完整可访问名称随 locale 同步变化。
- 本任务未改变锚点 ID、公开文案、公司资料、隐私处理、资源路径或发布流程。

### 未验证边界

- 尚未在真实 iOS Safari、Android Chrome 和 Firefox 设备上复核触控与系统辅助技术表现。
- 页面现有 GSAP 仍报告 `.scroll-arrow` 目标不存在的警告，该警告不由导航改动引入，本任务未扩展范围修复动画模块。

### 产品行为变化

- 有。主导航新增明确当前项和完整移动菜单关闭行为，语言切换的可访问名称保持本地化。

### 风险与回滚

- 风险集中在 860px 响应式边界和现有页面动画对初始锚点滚动的影响；回滚时可恢复 `index.html` 的菜单按钮结构、`assets/site.css` 的导航区和 `assets/site.js` 的菜单控制段，不需要迁移数据或修改部署配置。

## 2026-08-16 - 发布到 GitHub Pages

### 目标

- 将当前静态官网提交到公开 GitHub 仓库并完成可公网访问的 Pages 部署。

### 已验证

- 创建公开仓库 `https://github.com/Lanzxr/qiyunzhisuan-website`，本地 `master` 分支已跟踪 `origin/master`。
- GitHub Pages 已启用 `workflow` 构建方式，HTTPS 强制开启；Actions 运行 `31897718791` 的构建和部署作业成功。
- 公网首页 `https://lanzxr.github.io/qiyunzhisuan-website/`、CSS、JavaScript、透明 PNG 资源和 `?lang=zh-HK` 入口均返回 HTTP 200。
- 发布制品仍由 `.github/workflows/deploy-pages.yml` 白名单限制为 `index.html`、`assets/` 与 `robots.txt`。

### 未验证边界

- `REPLACE_WITH_META_TOKEN` 仍是占位值，Meta/Facebook 域名验证尚未执行。
- 尚未注册或绑定自定义域名，`CNAME`、canonical、`og:url` 与绝对 `og:image` 未配置。
- 当前任务未提供可调用的浏览器控制接口，因此公网验收为 HTTP 与资源层检查，未新增线上视觉截图。

### 运行变化

- 官网已从本地静态页面变为可通过 GitHub Pages HTTPS 公网访问；页面功能和公开文案未改变。

## 2026-08-16 - 使用透明 PNG 保留电脑边框和桌面光影

### 目标

- 在维持自然背景融合的同时恢复电脑边框、键盘和机身暗部细节。

### 已验证

- 从原 JPG 非破坏性生成 `workstation-development-cutout.png`，透明区域排除外部近黑环境，Alpha 遮罩保留电脑主体、边框和可见桌面反光。
- HTML 改用 1200x850 PNG；CSS 移除 `screen` 混合、亮度/对比度滤镜和椭圆 `mask-image`，原始颜色和细节不再被压淡。
- 原 JPG 保留在仓库中作为可回退素材；PNG 为新的公开页面资源。
- 本地 Chrome 在 `1337x1272` 桌面视口和 `390x844` 移动视口完成截图复核：无矩形或黑雾边界，电脑边框、键盘和屏幕颜色可见，主体尺寸克制，移动端不与上下内容重叠。

### 未验证边界

- 深色系统主题和 GitHub Pages 公网缓存仍需在部署后复核。

### 运行变化

- 更新 `index.html`、`assets/site.css` 和图片资源；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-16 - 压低图片近黑底色阶

### 目标

- 消除父层 screen 混合后媒体范围仍比背景轻微增亮的问题。

### 已验证

- 浏览器截图像素对比显示，媒体范围的空背景比 about 原背景高约 2 至 6 个 RGB 色阶；根因是 `contrast(0.92)` 抬高了素材近黑底。
- 图片对比度提高到 `1.34`，将近黑底压回黑色，使父层 `screen` 忽略暗部；图片尺度、位置和 contain 构图均不变。

### 未验证边界

- 修改后的本地 Chrome 截图需重新生成；深色系统主题、移动端真实浏览器和 GitHub Pages 公网缓存仍需单独复核。

### 运行变化

- 仅更新 `assets/site.css`；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-16 - 将混合模式提升到媒体父层

### 目标

- 修正图片在透明 figure 内先合成导致近黑底仍以矩形参与外层绘制的问题。

### 已验证

- `mix-blend-mode: screen` 从图片元素移到 `.about-media` 父层，使整个媒体合成结果直接与 about 渐变背景混合。
- 图片继续使用右侧局部 `contain` 构图，没有改变电脑主体大小或裁切位置；伪元素仍保持无背景。

### 未验证边界

- 修改后的本地 Chrome 截图需重新生成；深色系统主题、移动端真实浏览器和 GitHub Pages 公网缓存仍需单独复核。

### 运行变化

- 仅更新 `assets/site.css` 与关于区域稳定设计约束；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-16 - 修正 lighten 与透明度叠加造成的暗部压黑

### 目标

- 解决 `lighten` 与图片透明度叠加后仍压暗背景的问题，同时保持电脑主体的 contain 尺度。

### 已验证

- 图片层取消 `opacity` 合成，改由 `brightness(0.58)` 控制整体光影强度；`lighten` 只保留高于背景的屏幕和边缘光影，不再把近黑像素与透明黑混合成矩形暗层。
- 图片仍使用右侧局部 `contain` 构图，未通过放大或裁切改变电脑主体大小。

### 未验证边界

- 修改后的本地 Chrome 截图需重新生成；深色系统主题、移动端真实浏览器和 GitHub Pages 公网缓存仍需单独复核。

### 运行变化

- 仅更新 `assets/site.css`；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-16 - 保持关于区域图片主体尺度并恢复右侧局部构图

### 目标

- 修正全区域媒体层造成的电脑主体放大问题，同时保留渐变和图片的自然融合。

### 已验证

- 桌面端媒体层恢复为右侧局部宽度，图片改用 `object-fit: contain`，不再因整段区域的宽高比被 `cover` 放大或裁切。
- 图片主体改为右对齐，区域渐变继续承担从文字侧到图片侧的空间承接；`lighten` 混合和无伪元素覆盖层继续避免黑色矩形。
- 窄屏保持文字后的局部媒体，并将图片回到居中构图，避免手机视口裁掉主体。

### 未验证边界

- 修改后的 Chrome 截图需在本次代码更新后重新生成；深色系统主题、移动端真实浏览器和 GitHub Pages 公网缓存仍需单独复核。

### 运行变化

- 仅更新 `assets/site.css` 与关于区域稳定设计约束；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-16 - 改用 lighten 混合消除图片近黑底

### 目标

- 消除原图近黑像素在 `screen` 混合下形成的媒体矩形，让笔记本光影只在背景上增亮。

### 已验证

- `.about-media img` 改用 `lighten` 混合；低于区域背景亮度的近黑像素不会覆盖背景，屏幕和边缘光影仍可显示。
- 伪元素保持无背景，整段区域继续由 about 的深绿到近黑渐变承载。
- 本地 Chrome `1337x1272` about 截图已重新生成，页面结构和文字位置稳定。

### 未验证边界

- 截图验证使用本机 Chrome 明亮系统主题；深色系统主题、移动端真实浏览器和 GitHub Pages 公网缓存仍需单独复核。

### 运行变化

- 仅更新 `assets/site.css` 与关于区域稳定设计约束；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-16 - 移除造成矩形边界的媒体伪元素

### 目标

- 根据本地 Chrome 截图和隔离实验，消除关于区域媒体层顶部、底部和左右可见的矩形边界。

### 已验证

- 隔离实验中隐藏 `.about-media img` 后，矩形暗层仍然存在，确认边界来自 `.about-media::before`，而不是图片素材本身。
- 已移除桌面和移动端伪元素的叠加渐变；区域整体渐变保留，图片只使用右侧椭圆遮罩、`screen` 混合和低透明度光影。
- 使用本地 Chrome 以 `1337x1272` 视口滚动到 `#about` 截图，确认页面可以正常渲染，矩形伪元素不再参与绘制。

### 未验证边界

- 当前截图使用本机 Chrome 的明亮系统主题；深色系统主题、移动设备真实浏览器和 GitHub Pages 公网缓存仍需单独复核。

### 运行变化

- 仅更新 `assets/site.css` 与关于区域稳定设计约束；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-15 - 让关于区域媒体层覆盖整段并限制光影到右侧

### 目标

- 解决右侧图片容器起始位置仍可感知的问题，让笔记本光影与关于区域渐变处于同一背景层。

### 已验证

- 桌面端 `.about-media` 覆盖整个 `about` 区域，图片通过右侧椭圆遮罩和 `object-position` 保持视觉重心在右侧，不再存在媒体容器的左侧直角边界。
- `screen` 混合和透明度继续抹除图片黑底；文字仍位于前景层，图片不会改变文案排版或交互。
- 900px 以下恢复为文案后的局部媒体，并保留移动端上下渐隐。

### 未验证边界

- 当前环境没有可用的浏览器控制运行时，未能完成修改后的实际截图验收；需在目标浏览器刷新后确认笔记本位置、亮度和移动端裁切。

### 运行变化

- 仅更新 `assets/site.css` 与关于区域稳定设计约束；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-15 - 消除关于区域媒体层的矩形边界

### 目标

- 根据验收截图反馈，继续优化关于区域图片和渐变的关系，消除右侧媒体层可见的矩形暗边。

### 已验证

- `.about` 增加独立混合上下文；图片改用低透明度 `screen` 混合，使素材的黑色底色不再形成矩形色块，只保留笔记本的光影。
- 图片伪元素从左侧不透明起始色改为两端透明的轻微压暗层，保留区域渐变但不再绘制可见的容器边界。
- 移动端同步使用顶部透明、底部收束的纵向融合层。

### 未验证边界

- 当前环境没有可用的浏览器控制运行时，未能在本地页面上完成修改后的截图验收；需在浏览器强制刷新后确认不同缩放比例下的边界是否消失。
- 不支持 `mix-blend-mode` 或 `mask-image` 的旧浏览器会退化为普通图片层，融合效果可能较弱。

### 运行变化

- 仅更新 `assets/site.css` 与关于区域的稳定设计约束；不改变页面文案、语言切换、隐私处理或发布流程。

## 2026-08-15 - 将关于我们区域改为深绿至近黑的整体渐变

### 目标

- 根据截图反馈，解决关于我们区域背景与右侧图片边界明显的问题，让图片成为区域空间层次的一部分。

### 已验证

- `.about` 整段使用深绿色向右过渡到近黑色的渐变，文字、分隔线和图片融合层改用同一组区域专用颜色变量。
- 桌面端图片仍是右侧局部背景层，左侧通过同色横向渐隐，顶部和底部通过暗色收束；没有恢复独立图片卡片，也没有扩展为全页面背景。
- 900px 以下改用纵向渐变并将图片融合方向调整为上下过渡，避免窄屏出现横向边界。

### 未验证边界

- 当前会话没有可调用的浏览器截图接口，未完成桌面和移动端实际渲染截图；需在目标浏览器强制刷新后检查图片亮度、文字对比度和 about 到 contact 的连续性。
- CSS `mask-image` 在不支持的旧浏览器中会降级为图片层，边缘融合会弱于现代浏览器。

### 运行变化

- 仅更新 `assets/site.css` 和官网模块设计约束；不改变文案、语言切换、隐私处理或 GitHub Pages 发布流程。

## 2026-08-15 - 将关于图片改为局部背景层

### 目标

- 根据实际浏览器截图反馈，消除关于区域图片的矩形边界，让图片与文字背景形成更自然的空间关系。

### 已验证

- 桌面端 `.about-media` 不再参与网格排版，改为 `about-inner` 右侧的绝对定位局部层；文字保持前景层级。
- 图片使用椭圆 `mask-image`、左侧渐变、低饱和度和透明度，从四周衰减到 `about` 背景；不再有圆角、阴影或独立卡片边界。
- 900px 以下切回文案后的局部媒体，700px 以下高度进一步降低；`node --check assets/site.js`、媒体规则、资源路径和破折号规范检查通过。

### 未验证边界

- 当前会话仍无法自动生成浏览器截图；需在目标浏览器强制刷新后确认实际图片透明度是否符合偏好。

### 运行变化

- 仅更新 `assets/site.css` 和对应模块文档；不改变图片资源、文字内容、语言切换或页面锚点。

## 2026-08-15 - 优化隐私政策扫描布局与关于图片融合

### 目标

- 缩短隐私政策的纵向扫描距离，并让关于我们图片成为局部背景媒介而非独立图片卡片。

### 已验证

- 隐私政策保留标题、日期、说明和六段条款，桌面端条款改为右侧双列分组，标题侧栏在滚动时保持定位；900px 以下改为单列。
- 关于图片取消独立圆角卡片和阴影，使用局部固定高度、透明遮罩与上下左右渐变融合到 `about` section；移动端保留独立的局部媒体高度，不铺满页面。
- `node --check assets/site.js`、HTML 本地资源检查、隐私与关于区域 CSS 规则检查通过。

### 未验证边界

- 当前会话没有可调用的浏览器截图接口，修改后的桌面与移动端实际渲染仍需在目标浏览器刷新后复核。
- 图片融合依赖 CSS mask 和渐变；不支持 mask 的旧浏览器仍会显示局部图片，但边缘融合效果可能较弱。

### 运行变化

- 仅更新 `assets/site.css`；不改变政策文案、四语言字段、锚点或数据处理行为。

## 2026-08-15 - 重构繁体中文语言入口层级

### 目标

- 将台湾与香港繁体入口拆为主语言和地区两行，降低地区标签的视觉权重。

### 已验证

- 简体中文界面显示“繁体中文 / 台湾地区”和“繁体中文 / 中国香港”；台湾、香港与英语界面使用各自本地化地区称谓。
- 地区副标签采用 10px、较低对比度样式；按钮完整 `aria-label` 随 locale 更新。
- 700px 以下语言菜单由四列改为两列，避免英文主标签在窄屏溢出。
- `node --check assets/site.js`、全部 73 项四语言绑定检查，以及语言入口结构与响应式 CSS 静态检查通过。

### 未验证边界

- 修改后的桌面及移动端效果尚未在实际浏览器刷新后截图复核。

### 运行变化

- 更新 `index.html`、`assets/site.js` 和 `assets/site.css`；语言切换行为与 URL 状态规则不变。

## 2026-08-15 - 增加跨境业务自动化服务场景

### 目标

- 将 WhatsApp AI 客服与境外电商平台商品自动上下架融入官网服务体系，并保持信息层级与四语言本地化一致。

### 已验证

- 新增“跨境业务自动化”服务组，位于两项通用技术能力之后；桌面端横跨两列并分隔说明与功能，900px 以下收为单列。
- 简体中文、台湾繁体、香港繁体和英语均提供地区化标题、说明及两项功能表述；Meta description 同步纳入新增能力。
- `node --check assets/site.js`、四语言服务字段检查，以及服务区 HTML 绑定与响应式 CSS 静态检查通过。

### 未验证边界

- 当前会话未提供可调用的浏览器控制接口，未执行桌面或移动端截图验收；需在实际浏览器刷新后复核长文案换行与整体节奏。
- 本次仅更新官网展示文案与布局，不代表官网已接入 WhatsApp 或任何境外电商平台，也不提供在线自动化操作。

### 运行变化

- 更新 `index.html`、`assets/site.js` 和 `assets/site.css`；增加公开服务内容，不改变语言切换、隐私处理或发布流程。

## 2026-08-15 - 收紧 GitHub Pages 公开发布范围

### 目标

- 在尚未取得 Meta token 和自定义域名的前提下，完成其余 GitHub Pages 发布安全边界。

### 已验证

- 工作流改为在 CI 内生成 `_site`，只复制 `index.html`、`assets/` 和 `robots.txt`，`upload-pages-artifact` 不再上传仓库根目录。
- `.gitignore` 排除 `_site/`、`tmp/` 与常见系统缩略图文件；首图候选文件不会因常规 `git add .` 被提交。
- README 已改为说明发布白名单、Meta 标签验证方式、当前占位 token 的状态，以及 GitHub Pages 内容和 GitHub 仓库可见性的区别。
- `node --check assets/site.js`、HTML/CSS 本地资源路径检查、Pages 白名单定义检查，以及技术维护文档校验均已通过。

### 未验证边界

- Meta token、域名、DNS、HTTPS、canonical、`og:url` 和绝对 Open Graph 图片 URL 均未提供，尚未进行真实平台验收。
- GitHub 仓库设置为公开时，未被 Pages 发布的文件仍可能通过 GitHub 源码页面访问；需由仓库所有者选择合适的仓库可见性。
- 未在本机创建 CI staging 目录进行逐文件复制模拟：临时目录回收操作被本机命令安全策略拦截；工作流内容与全部已引用的本地资源已完成无写入静态校验。

### 运行变化

- 更新 `.github/workflows/deploy-pages.yml`、`.gitignore` 和 `README.md`；不改变页面内容、语言切换或数据处理行为。

## 2026-08-15 - 以文字品牌替换模拟 Logo

### 目标

- 移除未经确认的 `QY` 字母标记，让左上角公司名称成为主要视觉识别。

### 已验证

- `index.html` 已移除 `brand-mark` 元素，`assets/site.css` 也不再包含对应样式。
- 公司名称以本地化主文字显示，辅助英文名称或业务描述保持次级层级；桌面字号为 20px，移动端为 18px。
- 文字品牌保留 `brand.shortName` 和 `brand.descriptor` 的四语言绑定；静态检查和 `node --check assets/site.js` 通过。

### 未验证边界

- 正式 Logo、商标使用规范和品牌色尚未由公司提供；获得确认资产后可在不改变导航信息层级的前提下替换文字品牌。

### 运行变化

- 更新 `index.html` 和 `assets/site.css` 的页首品牌结构与响应式样式。

## 2026-08-15 - 校正香港繁体语法与术语

### 目标

- 将香港繁体由繁简转换式文案改为符合香港书面语习惯的官网文案。

### 已验证

- 香港版服务、联系、隐私和公司介绍已采用“企業數碼化、網上表格、發送電郵、聯絡資料、註冊地址”等香港常用表达。
- 香港版品牌显示为“啟雲智算”，浏览器标题和 Open Graph 标题为“啟雲智算｜人工智能及資訊科技服務”。
- 香港版关键文案不再包含“資訊化、面向、線上”等目标范围内的内地式表达；四种 locale 均保留全部 72 个页面本地化绑定。
- `node --check assets/site.js` 和香港措辞、字段完整性检查均通过。

### 未验证边界

- 法定公司名称仍保留已确认的工商登记原文；该名称不按香港繁体改写。
- 最终面向香港市场发布前，仍建议由当地母语使用者审核业务或法律术语的行业偏好。

### 运行变化

- 更新 `assets/site.js` 的香港繁体元信息、服务、公司介绍、联系和隐私文案。

## 2026-08-15 - 完成本地化文案覆盖

### 目标

- 确保四种语言不是仅切换字体或字符，而是对界面、品牌展示和业务文案进行真实本地化。

### 已验证

- 品牌短名和语言选择标签已纳入 `localeContent`，会随语言切换更新；台湾版使用“啟雲智算”，香港版使用“啟雲智算”，英文版使用 `Qiyun Zhisuang`。
- 页面全部 72 个 `data-i18n`、列表、配对内容、替代文字和无障碍标签在四种 locale 中均有对应字段。
- 公司法定名称、统一社会信用代码和注册地址保留已确认的登记原文；英文页面不伪造未经确认的官方英文登记名。
- `node --check assets/site.js` 与四语言字段覆盖检查通过。

### 未验证边界

- 最终台湾、香港措辞仍建议由熟悉当地用语的母语人员做发布前人工校对；当前版本已完成地区用词的结构化区分。

### 运行变化

- 更新 `index.html` 的品牌与语言切换绑定，扩展 `assets/site.js` 的本地化字段，并修正台湾、香港品牌用字。

## 2026-08-15 - 将隐私政策改为正式公开版本

### 目标

- 将首版简略隐私提示改为可在官网公开展示的分段政策，并保持四种语言同步。

### 已验证

- 隐私政策现明确适用范围、可能处理的信息、使用与保存、Cookie 与语言本地存储、共享与保护、个人权利、政策更新和联系邮箱。
- 页面不再包含“正式发布前确认最终法律文本”的临时措辞；中文、台湾繁体、香港繁体和英文均具备六个政策分段及对应内容。
- `node --check assets/site.js`、四语言字段完整性检查和隐私区结构检查均通过；窄屏断点已将政策布局收敛为单列。

### 未验证边界

- 内容仅基于当前静态网站的已知处理行为编写，未构成针对特定业务或司法辖区的法律意见。新增表单、统计、广告、支付或其他第三方服务前，必须复核并更新本政策。
- 发布前仍须以真实 Meta token、实际域名和 HTTPS 访问完成平台验证；这些配置未写入仓库。

### 运行变化

- 重构 `index.html` 的隐私区布局，扩展 `assets/site.js` 的四语言政策内容，并新增对应响应式样式。

## 2026-08-15 - 修复应用建设服务区亮色模式文字对比度

### 目标

- 修复“应用建设”深色服务区中列表文字被通用颜色规则覆盖的问题。

### 已验证

- 根因是 `.service-group li` 在源文件中晚于同等特异度的 `.service-group-primary li`，使亮色模式列表回退为深色 `--ink`。
- 已将深色服务区说明和列表的浅色文字规则置于通用列表规则之后，颜色为 `#d7eee5`。
- CSS 层叠顺序回归检查通过；文字与服务区背景的对比度为亮色模式 `9.96:1`、深色模式 `8.60:1`。

### 未验证边界

- 修复后的页面仍需在实际浏览器刷新后进行一次人工视觉复核。

### 运行变化

- 仅调整 `assets/site.css` 中服务区文字颜色的层叠顺序和颜色值。

## 2026-08-15 - 首页改为沉浸式代码背景

### 目标

- 采用用户确认的代码摄影素材作为首屏背景，移除独立图片卡片。

### 已验证

- 用户提供的 Unsplash 图片已本地化为 `assets/images/hero-technology-background.jpg`，页面不依赖运行时外站图片。
- `index.html` 已移除首屏 `figure.hero-art`，Open Graph 图片同步指向新的本地背景图。
- `assets/site.css` 通过首屏背景、左侧深色遮罩和按钮对比层承载文案；窄屏规则保留全屏首屏高度。
- 静态断言确认新图片引用、遮罩和旧卡片移除；`node --check assets/site.js` 通过。

### 未验证边界

- 当前会话没有可用的浏览器自动化接口，未执行桌面与移动端截图核验；最终发布前仍需在目标浏览器手动检查首屏裁切与文本对比度。
- 图片授权以用户提供的 Unsplash 素材来源为准；发布前由公司确认其使用范围。

### 运行变化

- 首屏不再展示人物或独立图片容器，背景图成为首屏的视觉环境。

## 2026-08-15 - 建立技术运维基线

### 目标

- 为 `website` 建立 interface、所有权和验证入口。

### 已验证

- 当前项目根目录为 `D:\Rui\Documents\ChatGPT\公司网站开发`，仅发现 `.git` 目录；没有页面源码、构建 manifest、测试入口或部署配置。
- 用户已确认首版用途：公司官网、Meta/Facebook 网站归属验证、公司真实性展示，以及多语言支持。
- 用户已提供公开服务方向：软件开发、人工智能应用开发、企业信息化解决方案、技术咨询服务。

### 未验证边界

- 域名、托管平台、HTTPS 证书、Meta Business 账号和验证方式尚未提供。
- 公司统一社会信用代码、注册地址、电话、邮箱、营业执照展示策略、英文公司名和品牌素材尚未提供。
- 技术栈、部署命令、实际页面验收和 Meta 验证均未执行；原因是当前仅获准先产出目标与实施计划。

### 运行变化

- 无。此条只建立技术文档基线。

## 2026-08-15 - 补充公司资料与静态多语言边界

### 目标

- 将用户提供的公司公开资料和四种静态语言范围纳入官网计划。

### 已验证

- 公司名称、成立时间、统一社会信用代码、注册地址和企业邮箱由用户提供。
- 公司简介明确覆盖人工智能应用、信息技术服务、软件开发、系统集成、数据处理、技术咨询等方向。
- 首版仅做静态展示页面，不接入动态数据或后台。

### 未验证边界

- 域名、托管、Meta Business 权限、英文法定名称、联系电话、Logo 和隐私政策最终文案仍未提供。

### 运行变化

- 无产品代码变化；仅更新目标与实施计划及维护文档。

## 2026-08-15 - 交付 GitHub Pages 静态官网首版

### 目标

- 实现可部署到 GitHub Pages 的四种语言静态官网，并保留 Meta 域名验证入口。

### 已验证

- 根目录 `index.html` 提供简体中文；`zh-tw/`、`zh-hk/`、`en/` 提供台湾繁体、香港繁体和英语静态页面。
- `assets/site.css` 提供响应式布局、系统明暗模式、焦点状态和 reduced-motion 降级；`assets/site.js` 只负责移动导航、视口进入动画和页脚年份，不加载业务数据。
- `assets/images/` 保存两张本地摄影素材，页面不依赖运行时外站图片。
- `.github/workflows/deploy-pages.yml` 提供 `main` 和 `master` 分支的 GitHub Pages Actions 发布流程。
- 根目录 `robots.txt` 允许公开爬虫访问；各语言页面保留 `facebook-domain-verification` 占位标签。

### 未验证边界

- GitHub 仓库、Pages 设置、自定义域名、HTTPS 证书和 Meta Business 验证尚未在真实账号上执行。
- Meta token、公司英文法定名称、联系电话、Logo 和最终隐私政策文本仍需公司确认。

### 运行变化

- 新增静态产品页面、样式、脚本、图片、GitHub Pages 工作流和项目 README。

## 2026-08-15 - 纠正首页信息层级与语言部署模型

### 目标

- 移除与公司服务重点不匹配的机房首屏图片，并避免以独立页面方式维护四种语言。

### 已验证

- 首屏改为团队协作摄影；公司主体信息集中在首屏后的核验区，服务按应用建设和企业信息化两组呈现。
- 根目录 `index.html` 是唯一公开页面入口；`assets/site.js` 的 `localeContent` 存放四种语言文案，语言按钮调用 `applyLocale` 原地更新页面和 `?lang=` 状态。
- 旧的 `zh-tw/`、`zh-hk/` 和 `en/` HTML 页面已移除，不再作为发布入口。

### 未验证边界

- 实际 GitHub Pages、自定义域名、HTTPS 和 Meta 验证仍需要在公司 GitHub 与 Meta 账号完成。
- 浏览器级视觉检查和四种语言的人工语义校对需要在最终目标浏览器中复核。

### 运行变化

- 重构 `index.html`、`assets/site.css` 和 `assets/site.js`，新增协作首屏图片，删除旧语言页面入口。

# Website维护日志

> 新条目放在最前；只新增，不改写历史结论。

## 2026-08-16 - 发布到 GitHub Pages

### 目标

- 将当前静态官网提交到公开 GitHub 仓库并完成可公网访问的 Pages 部署。

### 已验证

- 创建公开仓库 `https://github.com/Lanzxr/qiyunzhisuan-website`，本地 `master` 分支已跟踪 `origin/master`。
- GitHub Pages 已启用 `workflow` 构建方式，HTTPS 强制开启；Actions 运行 `31897718791` 的构建和部署作业成功。
- 公网首页 `https://lanzxr.github.io/qiyunzhisuan-website/`、CSS、JavaScript、透明 PNG 资源和 `?lang=zh-HK` 入口均返回 HTTP 200。
- 发布制品仍由 `.github/workflows/deploy-pages.yml` 白名单限制为 `index.html`、`assets/` 与 `robots.txt`。

### 未验证边界

- `REPLACE_WITH_META_TOKEN` 仍是占位值，Meta/Facebook 域名验证尚未执行。
- 尚未注册或绑定自定义域名，`CNAME`、canonical、`og:url` 与绝对 `og:image` 未配置。
- 当前任务未提供可调用的浏览器控制接口，因此公网验收为 HTTP 与资源层检查，未新增线上视觉截图。

### 运行变化

- 官网已从本地静态页面变为可通过 GitHub Pages HTTPS 公网访问；页面功能和公开文案未改变。

## 2026-08-16 - 使用透明 PNG 保留电脑边框和桌面光影

### 目标

- 在维持自然背景融合的同时恢复电脑边框、键盘和机身暗部细节。

### 已验证

- 从原 JPG 非破坏性生成 `workstation-development-cutout.png`，透明区域排除外部近黑环境，Alpha 遮罩保留电脑主体、边框和可见桌面反光。
- HTML 改用 1200x850 PNG；CSS 移除 `screen` 混合、亮度/对比度滤镜和椭圆 `mask-image`，原始颜色和细节不再被压淡。
- 原 JPG 保留在仓库中作为可回退素材；PNG 为新的公开页面资源。
- 本地 Chrome 在 `1337x1272` 桌面视口和 `390x844` 移动视口完成截图复核：无矩形或黑雾边界，电脑边框、键盘和屏幕颜色可见，主体尺寸克制，移动端不与上下内容重叠。

### 未验证边界

- 深色系统主题和 GitHub Pages 公网缓存仍需在部署后复核。

### 运行变化

- 更新 `index.html`、`assets/site.css` 和图片资源；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-16 - 压低图片近黑底色阶

### 目标

- 消除父层 screen 混合后媒体范围仍比背景轻微增亮的问题。

### 已验证

- 浏览器截图像素对比显示，媒体范围的空背景比 about 原背景高约 2 至 6 个 RGB 色阶；根因是 `contrast(0.92)` 抬高了素材近黑底。
- 图片对比度提高到 `1.34`，将近黑底压回黑色，使父层 `screen` 忽略暗部；图片尺度、位置和 contain 构图均不变。

### 未验证边界

- 修改后的本地 Chrome 截图需重新生成；深色系统主题、移动端真实浏览器和 GitHub Pages 公网缓存仍需单独复核。

### 运行变化

- 仅更新 `assets/site.css`；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-16 - 将混合模式提升到媒体父层

### 目标

- 修正图片在透明 figure 内先合成导致近黑底仍以矩形参与外层绘制的问题。

### 已验证

- `mix-blend-mode: screen` 从图片元素移到 `.about-media` 父层，使整个媒体合成结果直接与 about 渐变背景混合。
- 图片继续使用右侧局部 `contain` 构图，没有改变电脑主体大小或裁切位置；伪元素仍保持无背景。

### 未验证边界

- 修改后的本地 Chrome 截图需重新生成；深色系统主题、移动端真实浏览器和 GitHub Pages 公网缓存仍需单独复核。

### 运行变化

- 仅更新 `assets/site.css` 与关于区域稳定设计约束；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-16 - 修正 lighten 与透明度叠加造成的暗部压黑

### 目标

- 解决 `lighten` 与图片透明度叠加后仍压暗背景的问题，同时保持电脑主体的 contain 尺度。

### 已验证

- 图片层取消 `opacity` 合成，改由 `brightness(0.58)` 控制整体光影强度；`lighten` 只保留高于背景的屏幕和边缘光影，不再把近黑像素与透明黑混合成矩形暗层。
- 图片仍使用右侧局部 `contain` 构图，未通过放大或裁切改变电脑主体大小。

### 未验证边界

- 修改后的本地 Chrome 截图需重新生成；深色系统主题、移动端真实浏览器和 GitHub Pages 公网缓存仍需单独复核。

### 运行变化

- 仅更新 `assets/site.css`；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-16 - 保持关于区域图片主体尺度并恢复右侧局部构图

### 目标

- 修正全区域媒体层造成的电脑主体放大问题，同时保留渐变和图片的自然融合。

### 已验证

- 桌面端媒体层恢复为右侧局部宽度，图片改用 `object-fit: contain`，不再因整段区域的宽高比被 `cover` 放大或裁切。
- 图片主体改为右对齐，区域渐变继续承担从文字侧到图片侧的空间承接；`lighten` 混合和无伪元素覆盖层继续避免黑色矩形。
- 窄屏保持文字后的局部媒体，并将图片回到居中构图，避免手机视口裁掉主体。

### 未验证边界

- 修改后的 Chrome 截图需在本次代码更新后重新生成；深色系统主题、移动端真实浏览器和 GitHub Pages 公网缓存仍需单独复核。

### 运行变化

- 仅更新 `assets/site.css` 与关于区域稳定设计约束；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-16 - 改用 lighten 混合消除图片近黑底

### 目标

- 消除原图近黑像素在 `screen` 混合下形成的媒体矩形，让笔记本光影只在背景上增亮。

### 已验证

- `.about-media img` 改用 `lighten` 混合；低于区域背景亮度的近黑像素不会覆盖背景，屏幕和边缘光影仍可显示。
- 伪元素保持无背景，整段区域继续由 about 的深绿到近黑渐变承载。
- 本地 Chrome `1337x1272` about 截图已重新生成，页面结构和文字位置稳定。

### 未验证边界

- 截图验证使用本机 Chrome 明亮系统主题；深色系统主题、移动端真实浏览器和 GitHub Pages 公网缓存仍需单独复核。

### 运行变化

- 仅更新 `assets/site.css` 与关于区域稳定设计约束；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-16 - 移除造成矩形边界的媒体伪元素

### 目标

- 根据本地 Chrome 截图和隔离实验，消除关于区域媒体层顶部、底部和左右可见的矩形边界。

### 已验证

- 隔离实验中隐藏 `.about-media img` 后，矩形暗层仍然存在，确认边界来自 `.about-media::before`，而不是图片素材本身。
- 已移除桌面和移动端伪元素的叠加渐变；区域整体渐变保留，图片只使用右侧椭圆遮罩、`screen` 混合和低透明度光影。
- 使用本地 Chrome 以 `1337x1272` 视口滚动到 `#about` 截图，确认页面可以正常渲染，矩形伪元素不再参与绘制。

### 未验证边界

- 当前截图使用本机 Chrome 的明亮系统主题；深色系统主题、移动设备真实浏览器和 GitHub Pages 公网缓存仍需单独复核。

### 运行变化

- 仅更新 `assets/site.css` 与关于区域稳定设计约束；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-15 - 让关于区域媒体层覆盖整段并限制光影到右侧

### 目标

- 解决右侧图片容器起始位置仍可感知的问题，让笔记本光影与关于区域渐变处于同一背景层。

### 已验证

- 桌面端 `.about-media` 覆盖整个 `about` 区域，图片通过右侧椭圆遮罩和 `object-position` 保持视觉重心在右侧，不再存在媒体容器的左侧直角边界。
- `screen` 混合和透明度继续抹除图片黑底；文字仍位于前景层，图片不会改变文案排版或交互。
- 900px 以下恢复为文案后的局部媒体，并保留移动端上下渐隐。

### 未验证边界

- 当前环境没有可用的浏览器控制运行时，未能完成修改后的实际截图验收；需在目标浏览器刷新后确认笔记本位置、亮度和移动端裁切。

### 运行变化

- 仅更新 `assets/site.css` 与关于区域稳定设计约束；不改变文案、语言切换、隐私处理或发布流程。

## 2026-08-15 - 消除关于区域媒体层的矩形边界

### 目标

- 根据验收截图反馈，继续优化关于区域图片和渐变的关系，消除右侧媒体层可见的矩形暗边。

### 已验证

- `.about` 增加独立混合上下文；图片改用低透明度 `screen` 混合，使素材的黑色底色不再形成矩形色块，只保留笔记本的光影。
- 图片伪元素从左侧不透明起始色改为两端透明的轻微压暗层，保留区域渐变但不再绘制可见的容器边界。
- 移动端同步使用顶部透明、底部收束的纵向融合层。

### 未验证边界

- 当前环境没有可用的浏览器控制运行时，未能在本地页面上完成修改后的截图验收；需在浏览器强制刷新后确认不同缩放比例下的边界是否消失。
- 不支持 `mix-blend-mode` 或 `mask-image` 的旧浏览器会退化为普通图片层，融合效果可能较弱。

### 运行变化

- 仅更新 `assets/site.css` 与关于区域的稳定设计约束；不改变页面文案、语言切换、隐私处理或发布流程。

## 2026-08-15 - 将关于我们区域改为深绿至近黑的整体渐变

### 目标

- 根据截图反馈，解决关于我们区域背景与右侧图片边界明显的问题，让图片成为区域空间层次的一部分。

### 已验证

- `.about` 整段使用深绿色向右过渡到近黑色的渐变，文字、分隔线和图片融合层改用同一组区域专用颜色变量。
- 桌面端图片仍是右侧局部背景层，左侧通过同色横向渐隐，顶部和底部通过暗色收束；没有恢复独立图片卡片，也没有扩展为全页面背景。
- 900px 以下改用纵向渐变并将图片融合方向调整为上下过渡，避免窄屏出现横向边界。

### 未验证边界

- 当前会话没有可调用的浏览器截图接口，未完成桌面和移动端实际渲染截图；需在目标浏览器强制刷新后检查图片亮度、文字对比度和 about 到 contact 的连续性。
- CSS `mask-image` 在不支持的旧浏览器中会降级为图片层，边缘融合会弱于现代浏览器。

### 运行变化

- 仅更新 `assets/site.css` 和官网模块设计约束；不改变文案、语言切换、隐私处理或 GitHub Pages 发布流程。

## 2026-08-15 - 将关于图片改为局部背景层

### 目标

- 根据实际浏览器截图反馈，消除关于区域图片的矩形边界，让图片与文字背景形成更自然的空间关系。

### 已验证

- 桌面端 `.about-media` 不再参与网格排版，改为 `about-inner` 右侧的绝对定位局部层；文字保持前景层级。
- 图片使用椭圆 `mask-image`、左侧渐变、低饱和度和透明度，从四周衰减到 `about` 背景；不再有圆角、阴影或独立卡片边界。
- 900px 以下切回文案后的局部媒体，700px 以下高度进一步降低；`node --check assets/site.js`、媒体规则、资源路径和破折号规范检查通过。

### 未验证边界

- 当前会话仍无法自动生成浏览器截图；需在目标浏览器强制刷新后确认实际图片透明度是否符合偏好。

### 运行变化

- 仅更新 `assets/site.css` 和对应模块文档；不改变图片资源、文字内容、语言切换或页面锚点。

## 2026-08-15 - 优化隐私政策扫描布局与关于图片融合

### 目标

- 缩短隐私政策的纵向扫描距离，并让关于我们图片成为局部背景媒介而非独立图片卡片。

### 已验证

- 隐私政策保留标题、日期、说明和六段条款，桌面端条款改为右侧双列分组，标题侧栏在滚动时保持定位；900px 以下改为单列。
- 关于图片取消独立圆角卡片和阴影，使用局部固定高度、透明遮罩与上下左右渐变融合到 `about` section；移动端保留独立的局部媒体高度，不铺满页面。
- `node --check assets/site.js`、HTML 本地资源检查、隐私与关于区域 CSS 规则检查通过。

### 未验证边界

- 当前会话没有可调用的浏览器截图接口，修改后的桌面与移动端实际渲染仍需在目标浏览器刷新后复核。
- 图片融合依赖 CSS mask 和渐变；不支持 mask 的旧浏览器仍会显示局部图片，但边缘融合效果可能较弱。

### 运行变化

- 仅更新 `assets/site.css`；不改变政策文案、四语言字段、锚点或数据处理行为。

## 2026-08-15 - 重构繁体中文语言入口层级

### 目标

- 将台湾与香港繁体入口拆为主语言和地区两行，降低地区标签的视觉权重。

### 已验证

- 简体中文界面显示“繁体中文 / 台湾地区”和“繁体中文 / 中国香港”；台湾、香港与英语界面使用各自本地化地区称谓。
- 地区副标签采用 10px、较低对比度样式；按钮完整 `aria-label` 随 locale 更新。
- 700px 以下语言菜单由四列改为两列，避免英文主标签在窄屏溢出。
- `node --check assets/site.js`、全部 73 项四语言绑定检查，以及语言入口结构与响应式 CSS 静态检查通过。

### 未验证边界

- 修改后的桌面及移动端效果尚未在实际浏览器刷新后截图复核。

### 运行变化

- 更新 `index.html`、`assets/site.js` 和 `assets/site.css`；语言切换行为与 URL 状态规则不变。

## 2026-08-15 - 增加跨境业务自动化服务场景

### 目标

- 将 WhatsApp AI 客服与境外电商平台商品自动上下架融入官网服务体系，并保持信息层级与四语言本地化一致。

### 已验证

- 新增“跨境业务自动化”服务组，位于两项通用技术能力之后；桌面端横跨两列并分隔说明与功能，900px 以下收为单列。
- 简体中文、台湾繁体、香港繁体和英语均提供地区化标题、说明及两项功能表述；Meta description 同步纳入新增能力。
- `node --check assets/site.js`、四语言服务字段检查，以及服务区 HTML 绑定与响应式 CSS 静态检查通过。

### 未验证边界

- 当前会话未提供可调用的浏览器控制接口，未执行桌面或移动端截图验收；需在实际浏览器刷新后复核长文案换行与整体节奏。
- 本次仅更新官网展示文案与布局，不代表官网已接入 WhatsApp 或任何境外电商平台，也不提供在线自动化操作。

### 运行变化

- 更新 `index.html`、`assets/site.js` 和 `assets/site.css`；增加公开服务内容，不改变语言切换、隐私处理或发布流程。

## 2026-08-15 - 收紧 GitHub Pages 公开发布范围

### 目标

- 在尚未取得 Meta token 和自定义域名的前提下，完成其余 GitHub Pages 发布安全边界。

### 已验证

- 工作流改为在 CI 内生成 `_site`，只复制 `index.html`、`assets/` 和 `robots.txt`，`upload-pages-artifact` 不再上传仓库根目录。
- `.gitignore` 排除 `_site/`、`tmp/` 与常见系统缩略图文件；首图候选文件不会因常规 `git add .` 被提交。
- README 已改为说明发布白名单、Meta 标签验证方式、当前占位 token 的状态，以及 GitHub Pages 内容和 GitHub 仓库可见性的区别。
- `node --check assets/site.js`、HTML/CSS 本地资源路径检查、Pages 白名单定义检查，以及技术维护文档校验均已通过。

### 未验证边界

- Meta token、域名、DNS、HTTPS、canonical、`og:url` 和绝对 Open Graph 图片 URL 均未提供，尚未进行真实平台验收。
- GitHub 仓库设置为公开时，未被 Pages 发布的文件仍可能通过 GitHub 源码页面访问；需由仓库所有者选择合适的仓库可见性。
- 未在本机创建 CI staging 目录进行逐文件复制模拟：临时目录回收操作被本机命令安全策略拦截；工作流内容与全部已引用的本地资源已完成无写入静态校验。

### 运行变化

- 更新 `.github/workflows/deploy-pages.yml`、`.gitignore` 和 `README.md`；不改变页面内容、语言切换或数据处理行为。

## 2026-08-15 - 以文字品牌替换模拟 Logo

### 目标

- 移除未经确认的 `QY` 字母标记，让左上角公司名称成为主要视觉识别。

### 已验证

- `index.html` 已移除 `brand-mark` 元素，`assets/site.css` 也不再包含对应样式。
- 公司名称以本地化主文字显示，辅助英文名称或业务描述保持次级层级；桌面字号为 20px，移动端为 18px。
- 文字品牌保留 `brand.shortName` 和 `brand.descriptor` 的四语言绑定；静态检查和 `node --check assets/site.js` 通过。

### 未验证边界

- 正式 Logo、商标使用规范和品牌色尚未由公司提供；获得确认资产后可在不改变导航信息层级的前提下替换文字品牌。

### 运行变化

- 更新 `index.html` 和 `assets/site.css` 的页首品牌结构与响应式样式。

## 2026-08-15 - 校正香港繁体语法与术语

### 目标

- 将香港繁体由繁简转换式文案改为符合香港书面语习惯的官网文案。

### 已验证

- 香港版服务、联系、隐私和公司介绍已采用“企業數碼化、網上表格、發送電郵、聯絡資料、註冊地址”等香港常用表达。
- 香港版品牌显示为“啟雲智算”，浏览器标题和 Open Graph 标题为“啟雲智算｜人工智能及資訊科技服務”。
- 香港版关键文案不再包含“資訊化、面向、線上”等目标范围内的内地式表达；四种 locale 均保留全部 72 个页面本地化绑定。
- `node --check assets/site.js` 和香港措辞、字段完整性检查均通过。

### 未验证边界

- 法定公司名称仍保留已确认的工商登记原文；该名称不按香港繁体改写。
- 最终面向香港市场发布前，仍建议由当地母语使用者审核业务或法律术语的行业偏好。

### 运行变化

- 更新 `assets/site.js` 的香港繁体元信息、服务、公司介绍、联系和隐私文案。

## 2026-08-15 - 完成本地化文案覆盖

### 目标

- 确保四种语言不是仅切换字体或字符，而是对界面、品牌展示和业务文案进行真实本地化。

### 已验证

- 品牌短名和语言选择标签已纳入 `localeContent`，会随语言切换更新；台湾版使用“啟雲智算”，香港版使用“啟雲智算”，英文版使用 `Qiyun Zhisuang`。
- 页面全部 72 个 `data-i18n`、列表、配对内容、替代文字和无障碍标签在四种 locale 中均有对应字段。
- 公司法定名称、统一社会信用代码和注册地址保留已确认的登记原文；英文页面不伪造未经确认的官方英文登记名。
- `node --check assets/site.js` 与四语言字段覆盖检查通过。

### 未验证边界

- 最终台湾、香港措辞仍建议由熟悉当地用语的母语人员做发布前人工校对；当前版本已完成地区用词的结构化区分。

### 运行变化

- 更新 `index.html` 的品牌与语言切换绑定，扩展 `assets/site.js` 的本地化字段，并修正台湾、香港品牌用字。

## 2026-08-15 - 将隐私政策改为正式公开版本

### 目标

- 将首版简略隐私提示改为可在官网公开展示的分段政策，并保持四种语言同步。

### 已验证

- 隐私政策现明确适用范围、可能处理的信息、使用与保存、Cookie 与语言本地存储、共享与保护、个人权利、政策更新和联系邮箱。
- 页面不再包含“正式发布前确认最终法律文本”的临时措辞；中文、台湾繁体、香港繁体和英文均具备六个政策分段及对应内容。
- `node --check assets/site.js`、四语言字段完整性检查和隐私区结构检查均通过；窄屏断点已将政策布局收敛为单列。

### 未验证边界

- 内容仅基于当前静态网站的已知处理行为编写，未构成针对特定业务或司法辖区的法律意见。新增表单、统计、广告、支付或其他第三方服务前，必须复核并更新本政策。
- 发布前仍须以真实 Meta token、实际域名和 HTTPS 访问完成平台验证；这些配置未写入仓库。

### 运行变化

- 重构 `index.html` 的隐私区布局，扩展 `assets/site.js` 的四语言政策内容，并新增对应响应式样式。

## 2026-08-15 - 修复应用建设服务区亮色模式文字对比度

### 目标

- 修复“应用建设”深色服务区中列表文字被通用颜色规则覆盖的问题。

### 已验证

- 根因是 `.service-group li` 在源文件中晚于同等特异度的 `.service-group-primary li`，使亮色模式列表回退为深色 `--ink`。
- 已将深色服务区说明和列表的浅色文字规则置于通用列表规则之后，颜色为 `#d7eee5`。
- CSS 层叠顺序回归检查通过；文字与服务区背景的对比度为亮色模式 `9.96:1`、深色模式 `8.60:1`。

### 未验证边界

- 修复后的页面仍需在实际浏览器刷新后进行一次人工视觉复核。

### 运行变化

- 仅调整 `assets/site.css` 中服务区文字颜色的层叠顺序和颜色值。

## 2026-08-15 - 首页改为沉浸式代码背景

### 目标

- 采用用户确认的代码摄影素材作为首屏背景，移除独立图片卡片。

### 已验证

- 用户提供的 Unsplash 图片已本地化为 `assets/images/hero-technology-background.jpg`，页面不依赖运行时外站图片。
- `index.html` 已移除首屏 `figure.hero-art`，Open Graph 图片同步指向新的本地背景图。
- `assets/site.css` 通过首屏背景、左侧深色遮罩和按钮对比层承载文案；窄屏规则保留全屏首屏高度。
- 静态断言确认新图片引用、遮罩和旧卡片移除；`node --check assets/site.js` 通过。

### 未验证边界

- 当前会话没有可用的浏览器自动化接口，未执行桌面与移动端截图核验；最终发布前仍需在目标浏览器手动检查首屏裁切与文本对比度。
- 图片授权以用户提供的 Unsplash 素材来源为准；发布前由公司确认其使用范围。

### 运行变化

- 首屏不再展示人物或独立图片容器，背景图成为首屏的视觉环境。

## 2026-08-15 - 建立技术运维基线

### 目标

- 为 `website` 建立 interface、所有权和验证入口。

### 已验证

- 当前项目根目录为 `D:\Rui\Documents\ChatGPT\公司网站开发`，仅发现 `.git` 目录；没有页面源码、构建 manifest、测试入口或部署配置。
- 用户已确认首版用途：公司官网、Meta/Facebook 网站归属验证、公司真实性展示，以及多语言支持。
- 用户已提供公开服务方向：软件开发、人工智能应用开发、企业信息化解决方案、技术咨询服务。

### 未验证边界

- 域名、托管平台、HTTPS 证书、Meta Business 账号和验证方式尚未提供。
- 公司统一社会信用代码、注册地址、电话、邮箱、营业执照展示策略、英文公司名和品牌素材尚未提供。
- 技术栈、部署命令、实际页面验收和 Meta 验证均未执行；原因是当前仅获准先产出目标与实施计划。

### 运行变化

- 无。此条只建立技术文档基线。

## 2026-08-15 - 补充公司资料与静态多语言边界

### 目标

- 将用户提供的公司公开资料和四种静态语言范围纳入官网计划。

### 已验证

- 公司名称、成立时间、统一社会信用代码、注册地址和企业邮箱由用户提供。
- 公司简介明确覆盖人工智能应用、信息技术服务、软件开发、系统集成、数据处理、技术咨询等方向。
- 首版仅做静态展示页面，不接入动态数据或后台。

### 未验证边界

- 域名、托管、Meta Business 权限、英文法定名称、联系电话、Logo 和隐私政策最终文案仍未提供。

### 运行变化

- 无产品代码变化；仅更新目标与实施计划及维护文档。

## 2026-08-15 - 交付 GitHub Pages 静态官网首版

### 目标

- 实现可部署到 GitHub Pages 的四种语言静态官网，并保留 Meta 域名验证入口。

### 已验证

- 根目录 `index.html` 提供简体中文；`zh-tw/`、`zh-hk/`、`en/` 提供台湾繁体、香港繁体和英语静态页面。
- `assets/site.css` 提供响应式布局、系统明暗模式、焦点状态和 reduced-motion 降级；`assets/site.js` 只负责移动导航、视口进入动画和页脚年份，不加载业务数据。
- `assets/images/` 保存两张本地摄影素材，页面不依赖运行时外站图片。
- `.github/workflows/deploy-pages.yml` 提供 `main` 和 `master` 分支的 GitHub Pages Actions 发布流程。
- 根目录 `robots.txt` 允许公开爬虫访问；各语言页面保留 `facebook-domain-verification` 占位标签。

### 未验证边界

- GitHub 仓库、Pages 设置、自定义域名、HTTPS 证书和 Meta Business 验证尚未在真实账号上执行。
- Meta token、公司英文法定名称、联系电话、Logo 和最终隐私政策文本仍需公司确认。

### 运行变化

- 新增静态产品页面、样式、脚本、图片、GitHub Pages 工作流和项目 README。

## 2026-08-15 - 纠正首页信息层级与语言部署模型

### 目标

- 移除与公司服务重点不匹配的机房首屏图片，并避免以独立页面方式维护四种语言。

### 已验证

- 首屏改为团队协作摄影；公司主体信息集中在首屏后的核验区，服务按应用建设和企业信息化两组呈现。
- 根目录 `index.html` 是唯一公开页面入口；`assets/site.js` 的 `localeContent` 存放四种语言文案，语言按钮调用 `applyLocale` 原地更新页面和 `?lang=` 状态。
- 旧的 `zh-tw/`、`zh-hk/` 和 `en/` HTML 页面已移除，不再作为发布入口。

### 未验证边界

- 实际 GitHub Pages、自定义域名、HTTPS 和 Meta 验证仍需要在公司 GitHub 与 Meta 账号完成。
- 浏览器级视觉检查和四种语言的人工语义校对需要在最终目标浏览器中复核。

### 运行变化

- 重构 `index.html`、`assets/site.css` 和 `assets/site.js`，新增协作首屏图片，删除旧语言页面入口。
